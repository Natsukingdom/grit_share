<p>
  <strong>ニックネーム</strong>
  <%= @user.nickname %>
</p>

<p>
  <strong>メールアドレス</strong>
  <%= @user.email %>
</p>

<p>
  <strong>誕生日</strong>
  <%= @user.birthday %>
</p>


<%= link_to '今までのPomo', user_pomos_path(@user), class: 'btn btn-info' %>
<% if current_user == @user %>
  <%= link_to 'いますぐpomoする!', new_user_pomo_path(@user), class: 'btn btn-success' %>
  <%= link_to 'ユーザ情報を編集する', edit_user_registration_path(@user), class: 'btn btn-info' %>
<% end %>

<% if current_user&.admin?  %>
  <%= link_to 'ユーザ一覧画面をみる', users_path, class: 'btn btn-link' %>
<% end %>
